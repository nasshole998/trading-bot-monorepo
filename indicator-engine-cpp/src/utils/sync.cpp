#include "utils/sync.h"
// No implementation needed as this header only includes standard library primitives.
// Custom synchronization helpers would be implemented here if they were defined in sync.h.